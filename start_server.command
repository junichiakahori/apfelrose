#!/bin/bash
# このスクリプトが存在するディレクトリに移動
cd "$(dirname "$0")"

echo "=========================================================="
echo "   🌹 アプフェルローゼ 〜追憶のメロディ〜 簡易ローカルサーバー"
echo "=========================================================="
echo "サーバーを起動しています..."
echo "起動後、自動的にブラウザで http://localhost:3000 を開きます。"
echo "※終了するには、このウィンドウで Ctrl + C を押してください。"
echo "=========================================================="

# バックグラウンドで1秒待ってからブラウザでlocalhostを開く
(sleep 1 && open http://localhost:3000) &

# Python3でAPI対応簡易サーバーをポート3000番で起動
python3 server.py 3000
