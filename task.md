## 🔁 永続タスク（毎回の変更後に必ず実行）
- [ ] `version.json` のバージョン番号と `lastUpdated` を更新する
- [ ] `CHANGELOG.md` に変更内容を追記する
- [ ] `git add` → `git commit` → `git push` を実行する

---

## 完了済みタスク
- [x] `danmaku-shooting` 開発コードから `shooting` 本流ディレクトリへの統合 (`v1.0.1`)
- [x] 不要な旧「ハトの糞ゲー」のアセット・不要構成ファイルのクリーンアップ
- [x] モバイル/PC向け仮想ジョイスティックコントローラの実装 (`v1.0.2`)
- [x] ボス撃破後のストーリー進行が `gameState` の不整合で止まるバグの修正 (`v1.0.2`)
- [x] `index.html` にOgpタグ、Twitter Card、JSON-LD構造化データ等のSEO/OGPメタ情報を追加 (`v1.0.2`)
- [x] 検索エンジン用 `robots.txt` および `sitemap.xml` の作成 (`v1.0.2`)
- [x] `generate_image` を用いた「アプフェルローゼ」専用のOGPプレビュー画像 (`og-image.png`) の作成と配置 (`v1.0.2`)
